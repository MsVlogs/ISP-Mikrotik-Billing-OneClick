<x-app-layout>
@push('styles')<link rel="stylesheet" href="{{ asset('xlink-broadband/app.css') }}"><link rel="stylesheet" href="{{ asset('xlink-broadband/ui-polish-20260826.css') }}">@endpush
<div class="container-fluid py-2"><div class="d-flex justify-content-between align-items-center mb-3"><div><span class="text-uppercase small text-muted fw-semibold">Broadband</span><h3 class="mb-0">Broadband Packages</h3><small class="text-muted">Package profile, speed, price, router/pool mapping and availability.</small></div><a class="btn btn-primary" href="{{ route('package-list-setup') }}">Manage Packages</a></div>
<div class="card shadow-sm border-0"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Package Profile / Speed</th><th>Retail Price</th><th>Router / Pool</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($packages as $p)<tr><td><strong>{{ $p->package }}</strong></td><td>{{ number_format($p->price ?? 0,2) }} ৳</td><td>{{ $p->router_name ?? $p->router ?? 'Any / Area Router' }}</td><td><span class="badge text-bg-{{ ($p->status ?? 'active')==='active'?'success':'secondary' }}">{{ ucfirst($p->status ?? 'active') }}</span></td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('package-list-setup') }}">Edit</a></td></tr>@empty<tr><td colspan="5" class="text-center py-5">No packages configured.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $packages->links() }}</div></div></div>
</x-app-layout>
