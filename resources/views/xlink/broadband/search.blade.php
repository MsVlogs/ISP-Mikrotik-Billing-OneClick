<x-app-layout>
@push('styles')<link rel="stylesheet" href="{{ asset('xlink-broadband/customers.css') }}">@endpush
<div class="container-fluid py-2"><div class="d-flex justify-content-between align-items-center mb-3"><div><span class="text-uppercase small text-muted fw-semibold">Broadband</span><h3 class="mb-0">Customer Search</h3><small class="text-muted">Search by username, customer ID, mobile, IP, address or customer name.</small></div><a class="btn btn-primary" href="{{ route('customer-add') }}">Add Customer</a></div>
<div class="card shadow-sm border-0 mb-3"><div class="card-body"><form class="row g-2"><div class="col-md-8"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Username, Customer ID, Mobile, IP Address, Address or Name"></div><div class="col-md-4 d-flex gap-2"><button class="btn btn-primary">Search</button><a class="btn btn-outline-secondary" href="{{ url()->current() }}">Clear</a></div></form></div></div>
<div class="card shadow-sm border-0"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Customer ID</th><th>Customer</th><th>Username</th><th>Mobile</th><th>IP</th><th>Package</th><th>Status</th></tr></thead><tbody>@forelse($customers as $c)<tr><td>{{ $c->customer_unique_id }}</td><td><strong>{{ $c->customer_name }}</strong></td><td>{{ $c->pppUser->username ?? '—' }}</td><td>{{ $c->mobile ?: '—' }}</td><td>{{ $c->pppUser->ppp_remote_ip ?? '—' }}</td><td>{{ $c->package->package ?? $c->pppUser->package_name ?? '—' }}</td><td><span class="badge text-bg-{{ $c->status==='active'?'success':'secondary' }}">{{ ucfirst($c->status ?: 'unknown') }}</span></td></tr>@empty<tr><td colspan="7" class="text-center py-5">No customer found.</td></tr>@endforelse</tbody></table></div><div class="card-footer">{{ $customers->links() }}</div></div></div>
</x-app-layout>
